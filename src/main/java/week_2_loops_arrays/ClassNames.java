package week_2_loops_arrays;

public class ClassNames {
    public static void main(String[] args) {

        String[] classNames = {"Java", "Spanish", "Geography", "Diving"};

        for (int x = 0; x < classNames.length; x++) {
            {
                System.out.println(classNames[x]);
            }
        }
    }
}
